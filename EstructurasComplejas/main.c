#include <stdio.h>
#include <stdlib.h>

#include "Funciones.h"
#include "Estructuras.h"

int main(int argc, char const *argv[]) {
	Lista l;

	crear(&l);
	imprimir(&l);

	/*(a);
	free(b);
	free(c);*/
	return 0;
}